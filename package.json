{
  "name": "generator-washemo-20",
  "version": "0.0.1",
  "description": "Generador de aplicaciones verticales de Logtrust",
  "homepage": "http://google.com",
  "author": {
    "name": "david",
    "email": "david.jimenez19@gmail.com",
    "url": "home page"
  },
  "files": [
    "generator/app",
    "generator/verticalAppLegacy"
  ],
  "main": "generators/index.js",
  "keywords": [
    "generator",
    "yeoman-generator"
  ],
  "dependencies": {
    "chalk": "^1.0.0",
    "mkdirp": "^0.5.1",
    "path-exists": "^3.0.0",
    "react-webpack-template": "^1.0.0",
    "yeoman-generator": "^0.24.1",
    "yeoman-welcome": "^1.0.1",
    "yosay": "^1.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^7.0.0",
    "eslint": "^3.1.1",
    "eslint-config-xo-space": "^0.14.0",
    "eslint-plugin-react": "^6.3.0",
    "generate-changelog": "^1.0.2",
    "gulp": "^3.9.0",
    "gulp-coveralls": "^0.1.0",
    "gulp-eslint": "^2.0.0",
    "gulp-exclude-gitignore": "^1.0.0",
    "gulp-istanbul": "^1.0.0",
    "gulp-line-ending-corrector": "^1.0.1",
    "gulp-mocha": "^2.0.0",
    "gulp-nsp": "^2.1.0",
    "gulp-plumber": "^1.0.0",
    "yeoman-assert": "^2.0.0",
    "yeoman-test": "^1.0.0"
  },
  "eslintConfig": {
    "extends": "xo-space",
    "env": {
      "mocha": true
    }
  },
  "repository": "djimenezc/generator-washemo-20",
  "scripts": {
    "prepublish": "gulp prepublish",
    "release:major": "changelog -M && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version major && npm publish && git push --follow-tags",
    "release:minor": "changelog -m && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version minor && npm publish && git push --follow-tags",
    "release:patch": "changelog -p && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version patch && npm publish && git push --follow-tags",
    "test": "gulp",
    "test:watch": "gulp default watch"
  },
  "license": "MIT"
}
